name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    name: Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        go: ['1.25']
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: ${{ matrix.go }}
        cache: true

    - name: Download dependencies
      run: go mod download

    - name: Verify dependencies
      run: go mod verify

    - name: Configure Git (Windows)
      if: runner.os == 'Windows'
      shell: bash
      run: |
        git config --global core.askpass ""
        git config --global credential.helper ""
        git config --global init.defaultBranch main

    - name: Run tests (Unix)
      if: runner.os != 'Windows'
      run: go test -v -race -timeout=10m -coverprofile=coverage.txt -covermode=atomic ./...

    - name: Display coverage (Unix)
      if: runner.os != 'Windows'
      run: |
        echo "## Code Coverage Report (${{ matrix.os }})" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        go tool cover -func=coverage.txt >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        TOTAL=$(go tool cover -func=coverage.txt | grep total: | awk '{print $3}')
        echo "**Total Coverage: $TOTAL**" >> $GITHUB_STEP_SUMMARY

    - name: Run tests (Windows)
      if: runner.os == 'Windows'
      shell: bash
      env:
        GIT_TERMINAL_PROMPT: 0
      run: go test -v -timeout=10m -coverprofile=coverage.txt -covermode=atomic ./...

    - name: Display coverage (Windows)
      if: runner.os == 'Windows'
      shell: bash
      run: |
        echo "## Code Coverage Report (${{ matrix.os }})" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        go tool cover -func=coverage.txt >> $GITHUB_STEP_SUMMARY
        echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        TOTAL=$(go tool cover -func=coverage.txt | grep total: | awk '{print $3}')
        echo "**Total Coverage: $TOTAL**" >> $GITHUB_STEP_SUMMARY

  lint:
    name: Lint
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.25'
        cache: true

    - name: golangci-lint
      uses: golangci/golangci-lint-action@v9
      with:
        version: latest
        args: --timeout=5m

  build:
    name: Build
    runs-on: ubuntu-latest
    needs: [test, lint]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Set up Go
      uses: actions/setup-go@v6
      with:
        go-version: '1.25'
        cache: true

    - name: Build
      run: |
        go build -v -o foundagent ./cmd/foundagent
        ./foundagent version

    - name: Build for multiple platforms
      run: |
        GOOS=linux GOARCH=amd64 go build -o foundagent-linux-amd64 ./cmd/foundagent
        GOOS=darwin GOARCH=amd64 go build -o foundagent-darwin-amd64 ./cmd/foundagent
        GOOS=darwin GOARCH=arm64 go build -o foundagent-darwin-arm64 ./cmd/foundagent
        GOOS=windows GOARCH=amd64 go build -o foundagent-windows-amd64.exe ./cmd/foundagent
